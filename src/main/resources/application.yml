spring:
  datasource:
    url: jdbc:h2:file:./data/shieldlm
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
server:
  port: 8080
shieldlm:
  model:
    mode: mock
